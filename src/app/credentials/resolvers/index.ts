import { CredentialResolver } from "./credential.resolver";
import { CredentialsResolver } from "./credentials.resolver";

export const CREDENTIALS_RESOLVERS = [CredentialsResolver, CredentialResolver];
